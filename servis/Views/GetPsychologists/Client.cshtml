@model IEnumerable<servis.Models.Client>

@{
    ViewData["Title"] = "Выбор клиента";
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Year)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Phone)
            </th>
           
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Year)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>            
            <td>
     <form asp-action="ClientDetails">
        <input type="hidden" name="id" value="@ViewBag.Psychologist_id" />
         <input type="hidden" name="idC" value="@item.ID" />

        <input type="submit" value="Выбрать" class="btn btn-success" />
    </form> 
            </td>
        </tr>
}
    </tbody>
</table>
  <div>
    <a asp-action="Index">Обратно</a>
</div>
